<% if @results.nil? %>
<table class="ui celled table">
  <thead>
    <tr><th>ID</th>
    <th>タイトル（英語）</th>
    <th>タイトル（日本語）</th>
    <th>内容（英語）</th>
    <th>内容（日本語）</th>
    <th>位置</th>
    <th>URL</th>
    <th>画像</th>
    <th>動画</th>
    <th>スタート</th>
    <th>登録日</th>
    <th>編集日</th>
    <th>管理</th>
  </tr></thead>
  <tbody>
    <% @images.each do |image| %>
    <tr>
     <td><%= image.id %></td>
     <td><%= link_to "#{image.en_title}", image %></td>
     <td><%= image.ja_title %></td>
     <td><%= image.en_content %></td>
     <td><%= image.ja_content %></td>
     <td><%= image.location %></td>
     <td><%= image_tag(image.url, :size => '50x50') %></td>
     <td><%= image_tag(image.image, :size => '50x50') if !image.image.nil? %></td>
     <td><iframe width="560" height="315" src="https://www.youtube.com/embed/<%= image.movie %>" frameborder="0" allowfullscreen></iframe></td>
     <td><%= image.start %></td>
     <td><%= show_dat(image.created_at) %></td>
     <td><%= show_dat(image.updated_at) %></td>
     <td>
      <%= link_to "編集", edit_image_path(image.id), { :class => "ui olive button" } %>
      <%= link_to "削除", image, { :class => "ui red button", method: :delete, data: { confirm: "本当に削除してもいいですか？" }} %>
     </td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @images %>
<% end %>

<% if !@results.nil? %>
<table class="ui celled table">
  <thead>
    <tr><th>ID</th>
    <th>タイトル（英語）</th>
    <th>タイトル（日本語）</th>
    <th>内容（英語）</th>
    <th>内容（日本語）</th>
    <th>位置</th>
    <th>URL</th>
    <th>画像</th>
    <th>start</th>
    <th>key</th>
    <th>動画</th>
    <th>登録日</th>
    <th>編集日</th>
    <th>管理</th>
  </tr></thead>
  <tbody>
    <% @results.each do |image| %>
    <tr>
     <td><%= image.id %></td>
     <td><%= link_to "#{image.en_title}", image %></td>
     <td><%= image.ja_title %></td>
     <td><%= image.en_content %></td>
     <td><%= image.ja_content %></td>
     <td><%= image.location %></td>
     <td><%= image_tag(image.url, :size => '50x50') %></td>
     <td><%= image_tag(image.image, :size => '50x50') if !image.image.nil? %></td>
     <td><%= image.start %></td>
     <td><%= image.key %></td>
     <td><iframe width="300" height="200" src="https://www.youtube.com/embed/<%= image.movie %>" frameborder="0" allowfullscreen></iframe></td>
     <td><%= show_dat(image.created_at) %></td>
     <td><%= show_dat(image.updated_at) %></td>
     <td>
      <%= link_to "編集", edit_image_path(image.id), { :class => "ui olive button" } %>
      <%= link_to "削除", image, { :class => "ui red button", method: :delete, data: { confirm: "本当に削除してもいいですか？" }} %>
     </td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @results %>
<% end %>